{
  "name": "rift",
  "version": "0.0.0",
  "private": true,
  "description": "Rift framework monorepo",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "dev:counter": "pnpm --filter @rift/example-counter run dev"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.0.9"
}
