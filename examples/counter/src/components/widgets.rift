component Display {
  prop value = 0
  prop label = "Count"

  view {
    <p>
      <strong>{label}:</strong> <span>{value}</span>
    </p>
  }
}

component Stepper {
  prop label = "?"
  prop onstep = () => {}

  view {
    <button onclick={onstep}>{label}</button>
  }
}
